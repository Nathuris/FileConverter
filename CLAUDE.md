# CLAUDE.md

## 沟通语言

**始终使用简体中文与用户交流**（包括回复、说明、计划、代码注释与提交信息）。

- 不要用英文回复，即使是技术术语也优先给出中文表达（专有名词如 afconvert、ffmpeg、CoreAudio 可保留原文）。
- 代码注释沿用本仓库既有风格：简体中文。
- 报错信息面向中文用户，需翻译成可读的中文提示。

## 提交习惯

- **直接提交到 `main`，不要另开分支。**
- 只提交本次改动涉及的文件，不要顺手把 `.DS_Store` 之类的生成文件带上。

## 讲解方式

- **不要假设用户有编程背景**：用户完全没有计算机相关的知识和技能，一切内容都按“零基础”来讲。
- **尽量用自然语言描述**，避免术语。非用不可时（如“容器”“编码”“管道”），先用一句大白话说明它是什么、为什么重要，再往下讲。
- 多讲“发生了什么、为什么会这样、对你有什么影响”，少罗列代码层面的改动。
- 命令、参数、报错这类内容，要说明它在做什么，不要假设用户看得懂。
- 需要用户做决定时，用日常语言把选项和各自的后果讲清楚，而不是用技术名词让用户选。
